# 复制为 .env 或直接在 shell 中 export（勿将含密钥的 .env 提交版本库）
# 快照与 Agent 子目录日期（须与 data/snapshot_{该日}/ 及 父目录/{该日}/ 一致）
GOT_SNAPSHOT_DATE=2026-05-03
# Agent 各节点 JSON 的父目录（实际读取 父目录/GOT_SNAPSHOT_DATE/*.json）
GOT_AGENT_OUTPUT_DIR=
# 设为 1 时等价于 run_mvp --decision-only（仅刷新决策链读盘与 evaluation）
# GOT_DECISION_ONLY=

# 本地推理：子进程 argv（由 shlex 解析），整段「节点 .md + 上下文 JSON」提示词经 **stdin** 写入 UTF-8。
# 常见 Claude Code：GOT_LOCAL_INFER=claude -p -
# 超时秒数（每节点一次子进程）：GOT_LOCAL_INFER_TIMEOUT_SEC=900
# 子进程工作目录（可选）：GOT_LOCAL_INFER_CWD=
# GOT_LOCAL_INFER=
